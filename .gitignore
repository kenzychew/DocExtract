# Runtime data (inbox/processed/review/exports) and the local SQLite DB
data/

# Evaluation prediction cache (no benchmark data committed to git; build-plan 5.1)
eval/cache/

# Secrets and local environment
.env

# Virtual environment (uv manages this; uv.lock is committed instead)
.venv/

# Python caches and build artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Notebook checkpoints
.ipynb_checkpoints/

# NOTE: uv.lock and .python-version are intentionally committed (reproducible installs).
run_*.json
